imports:
    - { resource: config_dev.yml }

doctrine:
    dbal:
        host:     127.0.0.1
        dbname:   card_game_test
        user:     root
        password: null

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    disable_delivery: true
